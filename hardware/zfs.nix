{ config, ... }: {
  boot.extraModulePackages = with config.boot.kernelPackages; [ zfs ];

  services.zfs = {
    autoScrub = {
      enable = true;
      interval = "weekly";
    };
    trim = {
      enable = true;
      interval = "weekly";
    };
  };
}
