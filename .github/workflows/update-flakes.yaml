name: update-flakes
on:
  schedule:
    - cron:  '*/120 * * * *'
  workflow_dispatch:

permissions: {}

jobs:
  get-flakes:
    runs-on: ubuntu-latest
    outputs:
      flake-inputs: ${{ steps.get-flakes.outputs.flake-inputs }}
    steps:
      - uses: DeterminateSystems/nix-installer-action@v5
      - id: get-flakes
        run: |
          get_flake_inputs() {
            nix flake metadata --json "github:${GITHUB_REPOSITORY}/${GITHUB_SHA}" \
              | jq -c '
                  .locks.nodes.root.inputs
                  | {flake: keys}'
          }

          echo "flake-inputs=$(get_flake_inputs)" >> "$GITHUB_OUTPUT"
  update-flake:
    name: update-${{ matrix.flake }}
    runs-on: ubuntu-latest
    needs: get-flakes
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.get-flakes.outputs.flake-inputs) }}
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false
      - uses: DeterminateSystems/nix-installer-action@v5
      - run: git config --global user.email "hatesegfault@protonmail.ch"
      - run: git config --global user.name "hatesegfault"
      - uses: cpcloud/flake-update-action@v1.0.4
        with:
          dependency: ${{ matrix.flake }}
          pull-request-token: ${{ secrets.PAT }}
          pull-request-author: hatesegfault <hatesegfault@protonmail.ch>
          delete-branch: true
          github-token: ${{ secrets.GITHUB_TOKEN }}
          pull-request-branch-prefix: update-
          automerge: true
